import { Box, Container, Typography } from "@mui/material";
import React from "react";

const Home = () => {
  return (
    <Container component="div" sx={{ mt: 10 }}>
      <Typography variant="h3">Welcome To CRM Lead</Typography>
    </Container>
  );
};

export default Home;
